# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml

# Firebase / secrets — never commit credentials
google-services.json
GoogleService-Info.plist
.env

# Backend runtime data & secrets (never commit)
server/data/
node_modules/

# OS
.DS_Store
Thumbs.db

# Cloudflare API token (never commit)
deploy/.cf_token
